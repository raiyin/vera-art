-- Миграция 004: Удаление дублирующих таблиц images и videos
-- Таблицы images и videos дублировали информацию, которая уже хранилась
-- в полях images и videos таблицы news (в виде semicolon-разделённых строк).
-- SELECT'ов из этих таблиц не было, только INSERT'ы при добавлении новости.
-- Данные в этих таблицах не использовались, поэтому удаляем их.

-- 1. Удаляем таблицу images
DROP TABLE IF EXISTS images;

-- 2. Удаляем таблицу videos
DROP TABLE IF EXISTS videos;
