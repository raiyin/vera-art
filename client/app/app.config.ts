export default defineAppConfig({
    ui: {
        colors: {
            primary: 'green',
            neutral: 'slate',
        },
        colorMode: 'light', // default color mode (will be overridden by theme store)
    },
},);
